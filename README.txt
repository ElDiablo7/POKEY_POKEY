GRX PlayChips Poker (v0.1 scaffold)

- Chips-only. No cash. No wagering. No payouts.
- Deploy as static site (Render, Netlify, Vercel, etc.)
- Optional welcome video: assets/video/welcome.mp4

Run locally:
- Open index.html in a browser

Next bolt-ons:
1) Replace demo dealing with a real poker engine
2) Add WebSocket server for multiplayer tables
3) Add moderation + room creation + friend invites
4) Keep "chips" language everywhere (avoid cash terms)
